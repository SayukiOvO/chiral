# Binaries
/bin/
*.exe
*.out

# Go
vendor/

# SQLite data
*.db
*.db-wal
*.db-shm
/data/

# Env / secrets
.env
*.local
certs/

# Frontend
node_modules/
web/dist/*
!web/dist/.gitkeep
web/.vite/
web/*.tsbuildinfo

# Xray runtime assets (the downloaded/bundled kernel binary + geo data).
# Anchored to repo root so they do NOT match the agent/internal/xray/ source
# package (a bare "xray" would ignore that directory too).
/xray
/xray-core
geoip.dat
geosite.dat

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
