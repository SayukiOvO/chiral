version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # AgentFrame/CoreFrame carry direction semantics that
    # ChannelRequest/ChannelResponse would obscure.
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
