# deploy — 部署

Docker / docker-compose 部署文件。详见 [`../docs/deployment.md`](../docs/deployment.md)。

- `panel/` — Panel（中心）compose 与 Dockerfile
- `agent/` — 节点 compose 模板（Core「新增节点」时渲染）

**状态**：待起草（随 M1）。
