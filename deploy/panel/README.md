# deploy/panel — Panel 部署

`core` + 前端 + 可选 `caddy`/`nginx`(TLS) 的 docker-compose。

## 待办

- [x] `docker-compose.yml`（草案）
- [x] Dockerfile.core（草案；web 前端就绪后并入或单独起容器）
- [ ] `.env` 示例（关键变量见 compose 内 environment 注释）

**状态**：草案，随 M1 联调修订。
