# Chiral 面板配置示例
#
#   cp .env.example .env && $EDITOR .env && docker compose up -d
#
# compose 用 ${VAR:?...} 强制了必填项，缺了会在启动前就报错，而不是跑起来之后
# 才发现。带 ${VAR:-默认值} 的可以整行删掉。

# ── 必填 ─────────────────────────────────────────────────────────────

# Agent 拨回的地址，会写进「新增节点」生成的 compose 片段里。
# 必须是节点机器解析得到、且连得上的地址。
CHIRAL_GRPC_PUBLIC_ADDR=panel.example.com:26443

# 破窗用的 bearer token，与账号密码并行——密码全丢了也还能进。
#   openssl rand -base64 32
CHIRAL_ADMIN_TOKEN=

# 静态加密密钥。**丢了等于所有密文数据都解不开**：节点私钥、渲染后的 config、
# 用户凭证、告警配置、MFA 密钥、来源地址、订阅 token。
# 换它之前先跑 `chiral-core -rotate-secret-key`，见 docs/deployment.md。
#   openssl rand -base64 32
CHIRAL_SECRET_KEY=

# ── 首个管理员 ───────────────────────────────────────────────────────
#
# 只在库里一个管理员都没有时生效。密码留空则随机生成，并在启动日志里
# **只打印一次**——生产部署建议显式设置，否则得去翻日志。
CHIRAL_ADMIN_USER=admin
CHIRAL_ADMIN_PASSWORD=

# ── 反代 ─────────────────────────────────────────────────────────────
#
# 前面挂了 nginx / Caddy 时**必填**：逗号分隔的地址或 CIDR，只写那一层反代。
# 不设 → 限流按对端地址计，反代后所有人共用一个桶。
# 设错 → 任何调用方一个 X-Forwarded-For 就能自选桶，限流形同虚设。
# CHIRAL_TRUSTED_PROXY=172.18.0.0/16

# 面板自己的对外地址，用来拼订阅链接与认领链接；passkey 也需要它。
# CHIRAL_PUBLIC_URL=https://panel.example.com

# ── 端用户门户 ───────────────────────────────────────────────────────
#
#   off    门户路由整棵树不注册（默认）
#   closed 订阅者可登录，注册关闭；账号靠管理员发认领链接
#   open   开放注册，可用下面的注册码设门槛
#
# 非 off 时**必须**有 CHIRAL_SECRET_KEY，否则 Core 拒绝启动。
CHIRAL_PORTAL_MODE=off
# CHIRAL_PORTAL_INVITE_CODE=

# ── 在线地址记录 ─────────────────────────────────────────────────────
#
# 打开会给每个节点注入 statsUserOnline，也就是一次配置版本变更 + 一次 Xray
# 重启（该节点上的活连接会断一次）。默认关闭。
CHIRAL_ONLINE_RECORD=off

# ── 邮件（可选）──────────────────────────────────────────────────────
#
# 用于管理员的邮箱验证与登录验证码。HOST 留空即不提供邮件因素。
# 服务器不支持 STARTTLS 时会报错，而不是静默降级成明文。
# CHIRAL_SMTP_HOST=
# CHIRAL_SMTP_PORT=587
# CHIRAL_SMTP_USER=
# CHIRAL_SMTP_PASSWORD=
# CHIRAL_SMTP_FROM=chiral@example.com
# CHIRAL_SMTP_TLS=starttls

# ── gRPC 端 TLS（生产必需）───────────────────────────────────────────
#
# 不设则 Agent 连接是明文的。开发期 Agent 需要配合 --insecure。
# CHIRAL_TLS_CERT=/certs/fullchain.pem
# CHIRAL_TLS_KEY=/certs/privkey.pem
# ── 镜像来源 ────────────────────────────────────────────────────────
#
# 用本项目发布的镜像时这两项都不用动，默认值就是它们。自己 fork、自建
# registry、或者想把版本钉死时才设。
#
# CHIRAL_AGENT_IMAGE 要跟你实际发布的地方一致：控制台「新增节点」生成的
# compose 片段就用它，写错的话运维复制粘贴之后才在节点机器上失败——离出错
# 现场已经隔了一台机器和几分钟。
# CHIRAL_CORE_IMAGE=moonwx/chiral-core:latest
# CHIRAL_AGENT_IMAGE=moonwx/chiral-agent:latest

# 拉取策略。默认 always：大家跑的是 :latest，而 compose 默认会一直用第一次拉下来
# 的那份，于是 `up -d` 看起来成功、实际什么都没升。钉了具体版本号时可以设成
# missing，省一次 registry 往返。
# CHIRAL_PULL_POLICY=always

# ── 前端资源 ────────────────────────────────────────────────────────
#
# 留空。前端已经编译进二进制（门户 `/`，管理台 `/admin/`），这个变量会**覆盖**
# 内嵌副本。只有自己挂载一份构建产物时才设——指向镜像里不存在的路径会让两个
# 入口全部 404，而 Core 只会发一条告警。
# CHIRAL_WEB_DIR=

# ── Xray-core 运行时升级 ─────────────────────────────────────────────
#
# 面板给每个「机队里在跑的版本」保存一份二进制，用来校验那台节点的配置。
# 必须落在数据卷上：`xray -test` 只对跑它的那个 build 有效，节点升级之后
# 面板需要的是「那个」build。放容器本地路径，重启就忘光，面板会拿镜像里
# 烘焙的那一个去校验一个已经升级过的机队——升级带来的新特性全被当成未知
# 字段拒绝，而且是静默的，只表现为几天后一次说不清原因的下发失败。
#
# 镜像里已经设成 /var/lib/chiral/kernels，通常不用动。
# CHIRAL_KERNEL_DIR=/var/lib/chiral/kernels

# 升级后的验证要「真的上一次网」才算数：agent 用被测的那个二进制起一个一次性
# 客户端内核，经本机 SOCKS 打到本节点自己的 inbound，再取一次这个 URL。字节
# 回来了才判 ACTIVE。
#
# 原来的判据是问内核自己的 API，实测在一个没有任何人能上网的节点上照样应答
# （docs/xray-upgrade.md §2(c)），那恰好就是升级会造成的故障形状。
#
# 改它的唯一理由：默认这个地址从你的机房出不去。那样每一次金丝雀都会是
# INCONCLUSIVE——不会误回滚，但也永远确认不了任何事。整个机队共用这一个值。
# CHIRAL_PROBE_URL=http://cp.cloudflare.com/generate_204

# 可选。匿名 GitHub API 每 IP 每小时 60 次，单面板远远碰不到；共享出口 IP
# （公司网络、某些 VPS 机房）可能会——碰到之后「有新版本可升」就不再出现，
# 而且看起来像是「本来就没有新版本」。
# 只需要 public_repo 读权限，或者一个完全无权限的 fine-grained token。
# CHIRAL_GITHUB_TOKEN=
