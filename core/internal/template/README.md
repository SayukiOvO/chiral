# template — 模板引擎与变量池

变量池（全局 / 节点 / Profile / 用户 四作用域 + 生成器）、config 模板渲染、客户端模板渲染、`xray -test` 校验、config 版本化与回滚。

参考 [`../../../docs/template-system.md`](../../../docs/template-system.md)（Profile 抽象**待议**）。

**状态**：待实现（M2）。
