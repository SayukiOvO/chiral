# subscription — 订阅渲染

订阅 API（`/sub/{token}`）；按 User-Agent / URL 参数选客户端模板；渲染 clash / xray / v2rayN(vless) / stash 输出，填入用户的独立凭证。

参考 [`../../../docs/user-management.md`](../../../docs/user-management.md)。

**状态**：待实现（M3）。
